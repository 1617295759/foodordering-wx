<!--pages/mine/mine.wxml-->
<view class="userinfo">
      <image class="userinfo-avatar" 
      src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
      <button wx:if="{{userstate==1}}">去认证</button>
 </view>
 <view class="options">
 <button>消息</button>
 <button>更改信息</button>
 </view>
